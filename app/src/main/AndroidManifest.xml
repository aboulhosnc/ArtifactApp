<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.chady.artifactapp">

    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".Signup" />
        <activity android:name=".HomeScreen" />
        <activity
            android:name=".ArtifactsMainPage"
            android:label="@string/title_activity_artifacts_main_page"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name=".AddArtifact" />
        <activity android:name=".LoginActivity" />
        <activity android:name=".Collectors" />
        <activity android:name=".Settings" />
        <activity android:name=".SetupActivity" />
        <activity android:name=".ArtifactViewPage" />
        <activity
            android:name="com.theartofdev.edmodo.cropper.CropImageActivity"
            android:theme="@style/Base.Theme.AppCompat" />
        <activity android:name=".CalendarActivity" />
        <activity android:name=".AddEvent">

        </activity>
    </application>

</manifest>